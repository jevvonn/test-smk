function doubleNumbers(numbers) {
  // TODO: gunakan map untuk mengubah setiap nilai elemen
  // menjadi dua kali lipat nilai awal
  return [...numbers].map((num) => num * 2);
}

module.exports = doubleNumbers;
